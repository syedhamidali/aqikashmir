name: Refresh AQI Data

on:
  schedule:
    - cron: '0 * * * *'  # Every hour
  workflow_dispatch:      # Allow manual trigger
    inputs:
      past_days:
        description: 'Days of history to backfill in this run (max 92)'
        required: false
        default: '1'

concurrency:
  group: refresh-aqi-data
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Test API connectivity
        run: npm test

      - name: Fetch and log AQI data
        env:
          AQI_PAST_DAYS: ${{ github.event.inputs.past_days || '1' }}
        run: npm start

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add output/

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Refresh AQI data [skip ci]"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push origin HEAD:"${GITHUB_REF_NAME}"
          fi
